using UnityEngine;

public class GaussianSplatTester : MonoBehaviour
{
    private void Update()
    {
        if (enabled && Input.GetKeyDown(KeyCode.L))
        {
            // TODO:CW debug code
        }
    }
}
